
<div class="row">
  <h3>Author: <%= @author.name %> <%= @author.age %></h3>
</div>
<div class="col-md-12">
  <% @books.each do |book| %>
  <div class="row">
    <h4>Title: <%= book.title %></h4>
    <h4>desc: <%= book.desc %></h4>
  </div>
  <% end %>
</div>
<%= link_to 'Edit', edit_author_path(@author) %> 
<%= link_to 'Back', authors_path %>
<%= link_to 'add book', new_book_path %>
